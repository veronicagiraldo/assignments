const [value, setValue] = React.useState('')
const onChange = event => setValue(event.taret.value);
// return

// life cycle methods 
React.useEffect(() => {
  
})
// localStorage as a state 
// rules: not in loops, dont need to use classes 
// no classes with hooks  

// useReducer()
// {data, isLoading, }= useFetch(url)
//  with -class 
// collection of react hooks 
