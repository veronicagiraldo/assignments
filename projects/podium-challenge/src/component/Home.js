import React from 'react'
import {withTheme} from '../ThemeProvider';

const Home = () => {
  return (
    <div>
      Home info 
      {/* explain reviews */}
    </div>
  )
}

export default withTheme(Home);
